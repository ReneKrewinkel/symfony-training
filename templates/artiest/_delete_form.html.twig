<form method="post" action="{{ path('app_artiest_delete', {'id': artiest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artiest.id) }}">
    <button class="btn">Delete</button>
</form>
